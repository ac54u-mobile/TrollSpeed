name: Xcode - Build and Analyze

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  XCODE_VERSION: '15.4'

jobs:
  build:
    name: Build and analyse using xcodebuild command
    runs-on: macos-14

    steps:
      - name: Setup Xcode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Checkout
        uses: actions/checkout@v5

      - name: Build
        shell: bash
        run: |
          set -o pipefail
          xcodebuild clean build analyze -scheme TrollSpeed.Sim -project TrollSpeed.xcodeproj | xcpretty
